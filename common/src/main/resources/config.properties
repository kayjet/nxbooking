#quartz setting.
quartz.driverClassName=${quartz.driverClassName}
quartz.url=${quartz.url}
quartz.username=${quartz.username}
quartz.password=${quartz.password}
quartz.minPoolSize=${quartz.minPoolSize}
quartz.initialPoolSize=${quartz.initialPoolSize}
org.terracotta.quartz.skipUpdateCheck=${org.terracotta.quartz.skipUpdateCheck}

#database setting.
database.url=${database.url}
database.name=${database.name}
database.prefix=${database.prefix}
database.user=${database.user}
database.password=${database.password}

#redis
redis.host=${redis.host}
redis.port=${redis.port}
redis.password=${redis.password}
redis.maxIdle=${redis.maxIdle}
redis.maxTotal=${redis.maxTotal}
redis.maxWaitMillis=${redis.maxWaitMillis}
redis.blockWhenExhausted=${redis.blockWhenExhausted}
redis.testOnBorrow=${redis.testOnBorrow}
redis.timeout=${redis.timeout}
defaultCacheExpireTime=${defaultCacheExpireTime}

proxy.context=${proxy.context}
ws.address=${ws.address}
wechat.order.ip=${wechat.order.ip}
wechat.callback=${wechat.callback}
wechat.isdebug=${wechat.isdebug}


jobs.add.url=${jobs.add.url}
jobs.remove.url=${jobs.remove.url}